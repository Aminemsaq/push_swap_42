#ifndef PUSH_SWAP_H
#define PUSH_SWAP_H

#include <stdio.h>
#include <unistd.h>
#include <../libft/libft.h>

char **joining(int ac, char **av);
void free_double(char **tbl);
int help_join(char *number);

#endif